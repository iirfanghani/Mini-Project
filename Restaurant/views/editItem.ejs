<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>FoodXpress</title>
  <style>
    @import url("https://fonts.googleapis.com/css?family=Poppins:400,700");

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    body {
        min-height: 100vh;
        background: #f1f1f1;
    }

    .header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        padding: 20px 100px;
        background: #bc2f2f;
        display: flex;
        justify-content: space-between;
        align-items: center;
    } 

    .logo {
        color: #f1f1f1;
        font-size: 25px;
        text-decoration: none;
        font-weight: 600;
        cursor: default;
    }

    .navbar a {
        color: #f1f1f1;
        font-size: 18px;
        text-decoration: none;
        margin-left: 35px;
        transition: all 0.3s ease;
    }

    .navbar a:hover {
        color: #333;
    }

    .edit-item-form {
        max-width: 600px;
        margin: auto;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-group label {
        display: block;
        margin-bottom: 5px;
    }

    .form-group input[type="text"],
    .form-group input[type="file"] {
        width: 100%;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    .current-image {
        display: block;
        margin-top: 10px;
        max-width: 100%;
        height: auto;
    }

    .btn {
        display: inline-block;
        background-color: #007bff;
        color: white;
        padding: 10px 20px;
        text-decoration: none;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

    .btn:hover {
        background-color: #0056b3;
    }

    .update-btn, .back-btn {
        padding: 10px 15px; 
        font-size: 13px; 
        border: none; 
        border-radius: 5px; 
        display: inline-block; 
        text-align: center; 
        min-width: 100px; 
        cursor: pointer;/
    }
    
    .update-btn {
        background-color: #28a745; 
    }
    
    .back-btn {
        background-color: #6c757d; 
    }

    .no-item-found {
        color: #dc3545; 
    }

    .update-header {
        text-align: center; 
        font-size: 25px; 
        padding-top: 70px; 
        color: #333; 
    }
  </style>
</head>
  <body>
      <header class="header">
          <a href="#" class="logo">FoodXpress</a>
          <nav class="navbar">
              <a href="/home">Home</a>
              <a href="/menu">Menu</a>
              <a href="/contact">Contact</a>
          </nav>
      </header>

    <div>
        <br>
        <div class="update-header"><h2>Update Item</h2></div>
        <br>
        <div>
            <% if (menu) { %> 
                <form action="/editItem/<%= menu.itemId %>" method="POST" enctype="multipart/form-data" class="edit-item-form">
                    <div class="form-group">
                        <label for="name">Item Name:</label>
                        <input type="text" id="name" name="name" value="<%= menu.name %>" required>
                    </div>
        
                    <div class="form-group">
                        <label for="category">Category:</label>
                        <input type="text" id="category" name="category" value="<%= menu.category %>" required>
                    </div>
        
                    <div class="form-group">
                        <label for="price">Price:</label>
                        <input type="text" id="price" name="price" value="<%= menu.price %>" required>
                    </div>
        
                    <div class="form-group">
                        <label>Current Image:</label>
                        <input type="text" name="currentImage" value="<%= menu.image %>" disabled>
                        <img src="/images/<%= menu.image %>" alt="Current Image" class="current-image">
                    </div>
        
                    <div class="form-group">
                        <label for="image">New Image:</label>
                        <input type="file" id="image" name="image" accept="image/*">
                    </div>
        
                    <button type="submit" class="btn update-btn">Update Item</button>

                    <a href="/menu" class="btn back-btn">Back</a>
                </form>
            <% } else { %>
                <p class="no-item-found">No Item found.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
  