<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>FoodXpress</title>
    <style>
        @import url("https://fonts.googleapis.com/css?family=Poppins:400,700");

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            min-height: 100vh;
            background: #f1f1f1;
        }

        .header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 20px 100px;
            background: #bc2f2f;
            display: flex;
            justify-content: space-between;
            align-items: center;
        } 

        .logo {
            color: #f1f1f1;
            font-size: 25px;
            text-decoration: none;
            font-weight: 600;
            cursor: default;
        }

        .navbar a {
            color: #f1f1f1;
            font-size: 18px;
            text-decoration: none;
            margin-left: 35px;
            transition: all 0.3s ease;
        }

        .navbar a:hover {
            color: #333;
        }

        /* Modern Styles Below */
        .container {
            position: absolute; /* Changed to absolute positioning */
            top: 50%; /* Center vertically */
            left: 50%; /* Center horizontally */
            transform: translate(-50%, -50%); /* Adjust to exact center */
            max-width: 800px;
            padding: 20px;
            background: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .text-center {
            margin-top: 10px;
            margin-bottom: 10px;
            text-align: center;
        }

        img {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
        }

        p {
            font-size: 16px;
            line-height: 1.6;
            color: #333;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <header class="header">
      <a href="#" class="logo">FoodXpress</a>
      <nav class="navbar">
          <a href="/home">Home</a>
          <a href="/menu">Menu</a>
          <a href="/contact">Contact</a>
      </nav>
    </header>
    <div class="container">
        <div class="text-center"><h2>Item Details</h2></div>
        <% if (item) { %> <!-- Check if menu data exists -->
            <div>
                <p><b>Name:</b><br> <%= item.name %></p>
                <p><b>Image:</b><br> <img src="/images/<%= item.image %>" width="20%"></p>
                <p><b>Category:</b><br> <%= item.category %></p>
                <p><b>Price:</b><br> $<%= item.price %></p> 
            </div>
        <% } else { %>
            <p>No item found.</p>
        <% } %>
        <div class="text-center">
            <a href="/menu" class="btn menu-btn">Back</a>
        </div>
    </div>
</body>
</html>
